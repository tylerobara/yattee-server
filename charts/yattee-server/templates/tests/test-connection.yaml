apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yattee-server.fullname" . }}-test-connection"
  labels:
    {{- include "yattee-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yattee-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
